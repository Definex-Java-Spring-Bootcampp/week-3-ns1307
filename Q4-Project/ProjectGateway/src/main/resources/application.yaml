server:
  port: 8084

spring:
  application:
    name: ProjectGateway
    cloud:
      gateway:
        routes:
          id: order-service
          uri: http://localhost:8087
          predicates:
            - Path=/users
